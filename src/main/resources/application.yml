#spring boot配置
spring:
  application.name: fundprod
  datasource:
    url: jdbc:mysql://localhost:3306/av?serverTimezone=GMT%2B8&characterEncoding=utf-8&useSSL=false
    username: root
    password: 1234567890
    driverClassName: com.mysql.cj.jdbc.Driver
    max-active: 20
    max-idle: 10
    testOnBorrow: true
    validationQuery: SZELECT 1

site:
#域名
  domain: thzav.com
#超时时间(ms)
  timeout:  300000
#休眠时间(ms)
  sleep.time: 500

#文件保存路径
file.save.dir: "C:/TaoHuaZu"
#爬虫线程数
spider.thread:  5

